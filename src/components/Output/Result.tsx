import React from "react";
import styled from "styled-components";
import { MainWrapper } from "../../styles/MainWrapper";
const Styled = styled(MainWrapper)`
  display: block;
`;

export default function Result() {
  return <Styled>Result</Styled>;
}
